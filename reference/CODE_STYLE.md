# Code Style

- Native ESM only.
- Strict TypeScript; no `any`.
- Prefer immutable data and explicit Result values.
- Constructor injection and composition roots; no service locator.
- Type-only imports use `import type`.
- One public package entry point.
