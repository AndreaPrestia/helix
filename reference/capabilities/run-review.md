# Capability: RunReview

- Stability: Experimental
- Description: Evaluates changes against requirements, policies and quality gates.

## Contract

Inputs and outputs are immutable, serializable values. Errors are explicit. Implementations must be deterministic unless the capability explicitly delegates to an AI provider.
