# ADR-0015 — Rich Aggregate Model

Status: Accepted

State transitions are expressed as aggregate behavior. Specifications and tasks protect their invariants, emit events, and expose immutable views rather than public mutable state.
