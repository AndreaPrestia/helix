# Branching Strategy

Use one branch per OpenSpec change: `change/<change-id>-<slug>`.
Commits SHOULD reference the change and task. Pull requests MUST link proposal, tests, and verification evidence.
